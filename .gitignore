# === Secrets — never commit ===
.env
.env.*
!.env.example
# Build-time config for the desktop app. No secrets: a public API URL and the
# auth mode, both of which CI needs in order to build a release.
!.env.desktop
*.pem
*.p12
*.pfx
*.key
cwallet.sso
ewallet.p12
ewallet.pem
tnsnames.ora
sqlnet.ora
Wallet_*/
wallet/
secrets/

# === Dependencies ===
node_modules/
.pnpm-store/

# === Build output ===
dist/
build/
out/
*.tsbuildinfo
.vite/

# === Tauri / Rust ===
src-tauri/target/
src-tauri/gen/
# Generated from apps/web/public/icon-512.png by `pnpm tauri icon` during the
# release build. Committing .ico and .icns binaries that cannot be regenerated
# or reviewed here would be worse than generating them.
src-tauri/icons/

# === Local dev storage (uploaded images in dev) ===
.storage/
uploads/

# === Test / coverage ===
coverage/
.nyc_output/
test-results/
playwright-report/

# === Logs ===
*.log
npm-debug.log*
pnpm-debug.log*

# === OS / editor ===
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
