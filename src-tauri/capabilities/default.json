{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Only what the shell actually needs: opening an artist's external links in the system browser. No filesystem, shell or process access is granted.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:allow-open-url"]
}
